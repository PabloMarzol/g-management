# Essential dependencies for Streamlit Cloud
pandas>=2.0.0
plotly>=5.15.0
python-dotenv>=1.0.0

# Optional - only if you're actually using these
polars>=0.20.0

# Only include database deps if you're using a real database
# Comment out these lines if running demo-only mode
# sqlalchemy>=2.0.0
# asyncpg>=0.28.0
# psycopg2-binary>=2.9.0